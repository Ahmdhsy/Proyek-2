#ifndef SHIFTING_H
#define SHIFTING_H
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

char *shift_message(char *message, int shift_value);
//char *shift_word(char *word, int shift_value)
#endif
